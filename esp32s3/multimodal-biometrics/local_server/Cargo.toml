[package]
name = "biometric_server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.40", features = ["full"] }
axum = { version = "0.8.9", features = ["ws"] }
futures-util = "0.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# For Milestone 1 local AI processing:
# opencv = "0.92" (For face detection/recognition bindings)
# deepspeech = "0.9" or candle-core = "0.6" (For local speech processing)