[build]
target = "xtensa-esp32s3-espidf"

[target.'cfg(target_os = "espidf")']
linker = "ldproxy"
runner = "espflash flash --monitor"
rustflags = [ "--cfg",  "espidf_time64"]

[unstable]
build-std = ["std", "panic_abort"]

[env]
MCU = "esp32s3"
ESP_IDF_VERSION = "v5.5.3"
ESP_IDF_TOOLS_INSTALL_DIR = "global"
ESP_IDF_SYS_PYTHON_PATH = "/opt/homebrew/opt/python@3.12/bin/python3.12"

# The only component directory variable you need:
IDF_EXTRA_COMPONENT_DIRS = { value = "${CARGO_MANIFEST_DIR}/esp32-camera" }

# The include paths required by bindgen:
BINDGEN_EXTRA_CLANG_ARGS = { value = "-I${CARGO_MANIFEST_DIR}/esp32-camera/driver/include -I${CARGO_MANIFEST_DIR}/esp32-camera/conversions/include" }