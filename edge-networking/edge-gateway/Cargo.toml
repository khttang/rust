[package]
name = "edge-gateway"
version = "0.1.0"
edition = "2024"

[dependencies]
zero-trust-core = { path = "../zero-trust-core" }
zenoh = "1.9.0"
tokio = { version = "1.52.3", features = ["full"] }
futures = "0.3.32"
